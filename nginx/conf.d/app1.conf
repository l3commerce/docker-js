server {
    listen 80;
    server_name app1.localhost;

    resolver 127.0.0.11 valid=10s;
    resolver_timeout 5s;

    location / {
        set $upstream_app1 http://app1:3000;
        proxy_pass $upstream_app1;
    }
}
